/**
 * Single source of truth for Nextra locale config (page map, MDX routing).
 * Used by `next.config.mjs` and `instrumentation.ts`.
 */
export const NEXTRA_I18N = {
  locales: ["en", "tr"],
  defaultLocale: "en",
};
